# Production GPU inference deps for AMI depth_worker.
# Omits GUI/packaging packages from depth-estimation-repo/requirements_v2.txt
# (desktop-notifier, toga, pyinstaller) which need GObject/GTK system libs.
#
# GPU inference: onnxruntime-gpu (uses CUDA from depthworker/Dockerfile base image).
# PyTorch is CPU-only here — traptagger_api uses torch DataLoader/Dataset only;
# installing torch+cu118 pulls multi-GB nvidia/cu* pip wheels and bloats the image.

numpy<2.0.0
onnxruntime-gpu==1.18.0
opencv-python>=4.0.0
scikit-learn
scipy
platformdirs
--extra-index-url https://download.pytorch.org/whl/cpu
torch
torchvision
