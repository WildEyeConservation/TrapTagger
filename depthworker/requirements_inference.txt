# Production GPU inference deps for AMI depth_worker.
# Omits GUI/packaging packages from depth-estimation-repo/requirements_v2.txt
# (desktop-notifier, toga, pyinstaller) which need GObject/GTK system libs.
#
# Build with depthworker/Dockerfile (nvidia/cuda base). Do not install onnxruntime
# (CPU) alongside onnxruntime-gpu.

numpy<2.0.0
onnxruntime-gpu==1.18.0
opencv-python>=4.0.0
scikit-learn
scipy
platformdirs
transformers
accelerate
--extra-index-url https://download.pytorch.org/whl/cu118
torch
torchvision
